export {
    //   increase = () => {
    //     this.addToCount(1);
    //     this.getCount();
    // }

    // decrease = () => {
    //     this.addToCount(-1);
    //     this.getCount();
    // }

    // updateCount = () => {
    //     let s = (document.getElementById("textArea") as HTMLInputElement).value;
    //     let n = +s;
    //     this.setCount(n);
    // }

    // resetCounter = () => {
    //     this.setCount(0);
    //     this.getCount();
    // }

    // getCount = () => {
    //     fetch(ip + "/counter", {})
    //         .then(response => response.json())
    //         .then(data => this.setState((state) => ({ count: data.count })));
    // }

    // addToCount = (n: number) => {
    //     fetch(ip + "/counter/update", {
    //         method: 'POST',
    //         headers: { conetent: "application/json" },
    //         body: JSON.stringify({ upd: n })
    //     });
    // }

    // setCount = (n: number) => {
    //     fetch(ip + "/counter/reset", {
    //         method: 'PUT',
    //         headers: { conetent: "application/json" },
    //         body: JSON.stringify({ val: n })
    //     });
    // }
}